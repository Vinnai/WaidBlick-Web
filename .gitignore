.idea/
.vscode/
.DS_Store
Thumbs.db
*.log

# Lokale Notizen / Entwürfe
*.local.*
notes.local.md

# ML-Modelle und ONNX Runtime werden für GitHub Pages mit-committed
# (24 MB Mehrgewicht, dafür dauerhaft erreichbare URL).
# Bei künftigen Modell-Updates: einfach docs/models/best.onnx austauschen,
# committen, pushen — GitHub Pages aktualisiert sich automatisch.
